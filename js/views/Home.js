// Our landing page, default.
// Lets just make some html that's nice, and links to our other stuff.

export default function Home(props) {

}

// Will need to set buttons or a tags up to redirect from page, unless we just want to stick to the nav bar.

export function HomeEvents() {

}